---
label: Affiliate Code Service Integration
hide_body: true
fields:
- name: affiliateService
  type: field_group_list
  fields:
  - name: service
    type: select
    default: Amazon
    config:
      required: true
      options:
      - Amazon
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Service
    description: These are all the supported services. If you use a service that's
      not on this list contact, Authorize support (<a href="mailto:support@authorizewebsites.com"
      title="" target="_blank">support@authorizewebsites.com</a>) and we'll add support
      for it.
  - name: affiliateCode
    type: text
    config:
      required: true
    label: Affiliate Code
  config:
    min: 
    max: 
    labelField: 
  label: Affiliate Service
pages:
- forestry/integrations/affiliate-codes-service.json
